# UI fidelity status matrix

**Generated by `tools/generate_ui_status.py` — do not hand-edit.**
Reflects the working tree at `728c7406`.

Where Awake's UI stands against its targets — shadcn/ui for the design system, Radix
for headless behaviour, correct rendering for the engine — and what is still missing.
Each status is derived from a probe against the source (a marker grep, a file check, a
count), not from a written claim, so a row cannot report done for unwired work.

**9 done · 3 partial · 6 missing**

| Area | Status | Evidence | Verified by | Gap |
|---|---|---|---|---|
| Path tessellation | done | 2 reference(s) to the scanline filler | `UiPathFillTessellationTest` | Verified on the CPU rasterizer only; no real-backend pixel check. |
| Antialiasing | done | fringe centred via polygon offset | `UiPathFillTessellationTest` | MSAA is off on every UI pipeline; AA is geometric only. |
| Curve flattening | done | tolerance-driven | `UiPathFillTessellationTest` | — |
| Stroke rendering | partial | joins read | none | Blocks any outline icon tier; corners would leave open notches. |
| Icons | done | 356 cubic segments, generated from SVG | `tools/svg_to_ui_image_vector.py --self-test` | Fill-only: no outline tier until stroke joins exist. |
| Text metrics | done | advances pen-relative | `PackedUiFontAdvanceTest` | Baked raster atlas, size-quantised; no subpixel positioning. |
| Colour tokens | done | exact match, both modes | `ShadcnReferenceTokenExpandedTest` | Only the Vega preset on the Neutral base is checked. |
| Radius and spacing | done | theme-driven | `Shadcn*FidelityTest` | Seven non-default presets have no parity coverage. |
| Focus ring | **missing** | focused{} only recolours the border | none | shadcn draws a distinct 3px ring at 50% opacity outside the border. |
| Shadows | **missing** | `Style.shadow` exists, never called | `ShadcnCardVariantTest` | Card shadow-sm, button/input shadow-xs, dialog shadow-lg all absent. |
| Component size slot | **missing** | 9 hardcoded 40dp fallback(s) | none | shadcn's default control height is 36dp; sizes are not in the style contract. |
| Keyboard and focus | **missing** | keys exposed: Space | none | No Tab traversal, Escape dismiss, arrow navigation or Enter activation. One gap blocking roughly ten Radix behaviours. |
| Disabled states | done | 16 widget(s) dim when disabled | ui-headless widget tests | — |
| Semantics | done | 4 role(s) declared, 8 wired into widgets | ui-headless widget tests | No accessibility surface consumes these yet. |
| Reference capture | **missing** | 0 capture(s) from ui.shadcn.com | `tools/capture_shadcn_reference.py` | Light mode only. |
| Comparison harness | partial | 8/12 pair(s) aligned well enough to read | `ShadcnReferenceComparisonTest` | Still unreadable: slider-local-light, tooltip-local-light, card-local-light, tabs-local-light. |
| Dark mode parity | **missing** | 0 dark capture(s) | none | No dark reference captures exist; every parity preview is light. |
| Layer ownership | partial | 3 branded component(s) with no headless primitive | `verifyUiOwnership` | Behaviour for dialog, tabs, collapsible lives only in ui-designsystem, so it cannot be reused unbranded. |

## Reading this

- **Verified by `none`** means no automated test guards the area. A `done` row with no
  verifier is an assertion about the code as it stands today, not a regression guard.
- **Gap** is what remains even when the status is `done`; a done row can still carry a
  real limitation.
- Status is deliberately coarse. It answers whether the mechanism exists, not whether
  it is pixel-exact — `docs/reference/shadcn-parity.md` carries the measured numbers,
  and `docs/reference/ui-validation.md` the policy for what counts as proof.

