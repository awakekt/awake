# Awake Engine — MVP Plan

The live roadmap. Completed work is summarised here in one line per phase and recorded in full
in [archive/mvp-phase-log.md](archive/mvp-phase-log.md); decisions and their rationale live in
[reference/decision-log.md](reference/decision-log.md). Keep this file about what is *next*.

## Vision

The first Kotlin Multiplatform game engine with:

- a **Vulkan core** on Android, desktop (macOS/Windows/Linux) and iOS via MoltenVK
- a **WebGPU backend** for Web/Wasm behind the same renderer abstraction
- a shared **ECS runtime** in `commonMain`
- a **declarative, Compose-style scene API**
- a **desktop editor**
- C++ JNI bindings generated by [jni-binding-generator](https://github.com/ronjunevaldoz/jni-binding-generator)

## MVP definition

A textured, ECS-driven 3D scene running from one `commonMain` codebase on Android and desktop,
loaded from a serialized scene document, editable in a minimal editor. **This is met.** Work
below is what remains beyond it.

## Phase status

| Phase | State | Where it lives now |
|---|---|---|
| 0 Foundation modernization | done | Kotlin 2.4.10, AGP 9.2.1, Gradle 9.6.1, Compose MP 1.11.1, detekt gate, GitHub Actions CI |
| 1 Vulkan core on desktop | done | `awake:backend:vulkan`, `VulkanDesktopHost` |
| 2 Renderer abstraction | done | `awake:engine:render:contract`, `awake:engine:render:passes` |
| 2.5 Web/WebGPU backend | done | `awake:backend:webgpu`, including the directional shadow pass |
| 3 ECS | done | `awake:ecs`, with a benchmark suite in `awake:ecs:benchmark` |
| 4 Engine runtime | done | `awake:engine:platform` / `:app` / `:bootstrap` |
| 5 Editor | done, differently | `samples:studio` — hierarchy, inspector, gizmo, viewport, play mode. The original plan called for a separate `awake-editor` Compose Desktop app; a sample shell got there first and is what actually exists. |
| 6 iOS via MoltenVK | **not started** | `VulkanGameViewController` exists; no app target drives a frame loop, so nothing is hardware-verified |
| 7 Compose-style scene API | done | `awake:scene:authoring` — `app { }`, `scene { }`, `entity { }` |
| 8 Physics (Jolt) | partial | contract in `awake:physics:api`, bridge in `awake:backend:jolt`, `PhysicsSystem` in `awake:scene:physics`; no demo scene yet |
| 9 Audio | **not started** | no module exists |

## What's actually left

**Phase 6 — iOS via MoltenVK.** The only MVP-scope phase not done. Needs an iOS app target
driving a real frame loop, then hardware verification on a physical device rather than the
simulator. `IOSFrameLoop` is implemented but has never run.

**Phase 8 — finish physics.** The pieces exist but nothing demonstrates them end to end. Needs a
demo scene (falling boxes onto a ground plane) on Android and desktop. Batched transform sync
through a shared direct `ByteBuffer` / pinned memory is a later optimisation, not a blocker.

**Phase 9 — audio.** Not started, no module. `WindowLifecycle`'s doc comment sketches where an
`audio` accessor would attach when it begins.

**Smaller open items**

- Ktlint is still deferred — legacy code predates the formatting rules, so enabling it means a
  standalone `ktlintFormat` sweep.
- Adaptive bulk structural mutation in the ECS — see
  [tasks/2026-08-21-ecs-adaptive-bulk-mutation-plan.md](tasks/2026-08-21-ecs-adaptive-bulk-mutation-plan.md).
- Extract `awake:core:math` — see
  [reference/module-architecture.md](reference/module-architecture.md).
- WebGPU wireframe support is stored but never read, so the flag is inert on that backend.

## Risks

| Risk | Mitigation |
|---|---|
| JNI per-call marshalling overhead | Acceptable at MVP scene sizes; the renderer abstraction is the seam for batched command recording later |
| iOS binding path is entirely different (cinterop, not JNI) | The common API is already `expect`/`actual`-shaped; Phase 6 only has to supply actuals |
| Vulkan handles are raw `Long`s — leaks and use-after-free are silent | Wrapper classes own handles with explicit `Disposable` lifecycle; validation layers on in debug builds |
| Driver allocation limits (~4096 `vkAllocateMemory` calls) | VMA sub-allocation in the C++ layer |
| A rendering regression does not throw, it just looks slightly wrong | Pixel-baseline tests and the frame-timing harness in `:awake:compose:ui-testing` |

## Related

- [architecture.md](architecture.md) — module map and dependency rules
- [reference/decision-log.md](reference/decision-log.md) — D1–D25, why things are the way they are
- [archive/mvp-phase-log.md](archive/mvp-phase-log.md) — the full per-phase execution record
- [mmorpg-roadmap.md](mmorpg-roadmap.md) — post-MVP direction
