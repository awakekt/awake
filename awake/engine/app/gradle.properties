# Manual source-set hierarchy below (vulkanMain shared by android/ios/desktop, not wasmJs) --
# the default hierarchy template would create its own competing groups (e.g. iosMain) for the
# same targets and warn about the conflict.
kotlin.mpp.applyDefaultHierarchyTemplate=false
