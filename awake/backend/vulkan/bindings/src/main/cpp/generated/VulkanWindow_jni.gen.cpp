// GENERATED by jni-binding-generator.py, then hand-edited to fill in the native call
// bodies below. Regenerating overwrites this file; see docs/decisions/D10-codegen-
// derisk-findings.md for the re-merge procedure used to preserve hand-written bodies.
// Desktop-only (see VulkanWindow.kt's doc comment) -- only compiled into
// desktop-native's CMake build, not android-native's.
// Source: VulkanWindow.kt
//
// glfwSetScrollCallback/glfwConsumeScrollDeltaY (pinch-to-zoom, D-something scroll support):
// the generator only knows how to marshal "call a GLFW getter, return its value" -- GLFW
// scroll input is push/callback-based (glfwSetScrollCallback), which doesn't fit that shape.
// The JNI entry-point *signatures* for both functions are still generator-produced (regular
// per-function template); only their *bodies*, plus the file-scope static accumulator and
// awake_glfwScrollCallback() below, are hand-written outside the generator's normal
// "one GLFW call per function" assumption. Regenerating this file again will re-wipe both
// bodies to TODO stubs (same as every other hand-filled body here) but will NOT remove the
// hand-written static accumulator/callback function above the JNI exports, since those live
// outside any generated function block.

#include <jni.h>
#include <optional>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>
#include <cstdint>
// GLFW_INCLUDE_VULKAN is set as a CMake target_compile_definitions in
// desktop-native/CMakeLists.txt (so it's harmless if this file is ever compiled
// elsewhere without it -- it just wouldn't get glfwCreateWindowSurface's declaration).
#include <GLFW/glfw3.h>

#include "jni-utils.h"
#include "exception_utils.h"

// Hand-written (not part of the generator's per-function template -- GLFW scroll input is
// callback-based, not a simple polled getter, so this doesn't fit the generator's normal
// "call this GLFW getter, marshal its return value" shape; see VulkanWindow.kt's doc comment
// on glfwSetScrollCallback/glfwConsumeScrollDeltaY). A single process-wide accumulator is
// fine here for the same reason a single global GLFWwindow* would be: this codebase runs one
// GLFW window per process (see this project's threading-model doc). Not thread-guarded --
// the scroll callback fires synchronously inside glfwPollEvents(), and glfwConsumeScrollDeltaY
// is polled from the same render thread right after, per this project's "one thread owns
// every Vulkan/GLFW call" rule.
static double g_scrollAccumulatorY = 0.0;

static void awake_glfwScrollCallback(GLFWwindow* window, double xoffset, double yoffset) {
    (void)window;
    (void)xoffset;
    g_scrollAccumulatorY += yoffset;
}


extern "C" JNIEXPORT jboolean JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwInit(
        JNIEnv* env,
        jclass clazz) {
    return glfwInit() ? JNI_TRUE : JNI_FALSE;
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwTerminate(
        JNIEnv* env,
        jclass clazz) {
    glfwTerminate();
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwWindowHint(
        JNIEnv* env,
        jclass clazz,
        jint hint,
        jint value) {
    // --- Marshalling ---
    int32_t hint_val = static_cast<int32_t>(hint);
    int32_t value_val = static_cast<int32_t>(value);

    glfwWindowHint(hint_val, value_val);
}


extern "C" JNIEXPORT jlong JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwCreateWindow(
        JNIEnv* env,
        jclass clazz,
        jint width,
        jint height,
        jstring title) {
    // --- Marshalling ---
    int32_t width_val = static_cast<int32_t>(width);
    int32_t height_val = static_cast<int32_t>(height);
    std::string title_val = jstring2string(env, title);
    if (env->ExceptionCheck()) return 0;

    // --- Error handling ---
    if (title_val.empty()) {
        throw_illegal_argument(env, "glfwCreateWindow: title is required");
        return 0;
    }

    GLFWwindow* window = glfwCreateWindow(width_val, height_val, title_val.c_str(), nullptr, nullptr);
    if (!window) {
        throw_illegal_state(env, "glfwCreateWindow: window creation failed");
        return 0;
    }
    return reinterpret_cast<jlong>(window);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwDestroyWindow(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwDestroyWindow: window not initialized");
        return;
    }

    glfwDestroyWindow(reinterpret_cast<GLFWwindow*>(window_ptr));
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwFocusWindow(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwFocusWindow: window not initialized");
        return;
    }

    glfwFocusWindow(reinterpret_cast<GLFWwindow*>(window_ptr));
}


extern "C" JNIEXPORT jboolean JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwWindowShouldClose(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwWindowShouldClose: window not initialized");
        return JNI_FALSE;
    }

    return glfwWindowShouldClose(reinterpret_cast<GLFWwindow*>(window_ptr)) ? JNI_TRUE : JNI_FALSE;
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwPollEvents(
        JNIEnv* env,
        jclass clazz) {
    glfwPollEvents();
}


extern "C" JNIEXPORT jint JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetFramebufferWidth(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwGetFramebufferWidth: window not initialized");
        return 0;
    }

    int width = 0, height = 0;
    glfwGetFramebufferSize(reinterpret_cast<GLFWwindow*>(window_ptr), &width, &height);
    return static_cast<jint>(width);
}


extern "C" JNIEXPORT jint JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetFramebufferHeight(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwGetFramebufferHeight: window not initialized");
        return 0;
    }

    int width = 0, height = 0;
    glfwGetFramebufferSize(reinterpret_cast<GLFWwindow*>(window_ptr), &width, &height);
    return static_cast<jint>(height);
}


extern "C" JNIEXPORT jint JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetWindowWidth(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwGetWindowWidth: window not initialized");
        return 0;
    }

    int width = 0, height = 0;
    glfwGetWindowSize(reinterpret_cast<GLFWwindow*>(window_ptr), &width, &height);
    return static_cast<jint>(width);
}


extern "C" JNIEXPORT jint JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetWindowHeight(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwGetWindowHeight: window not initialized");
        return 0;
    }

    int width = 0, height = 0;
    glfwGetWindowSize(reinterpret_cast<GLFWwindow*>(window_ptr), &width, &height);
    return static_cast<jint>(height);
}


extern "C" JNIEXPORT jlong JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwCreateWindowSurface(
        JNIEnv* env,
        jclass clazz,
        jlong instance,
        jlong window) {
    // --- Marshalling ---
    void* instance_ptr = reinterpret_cast<void*>(instance);
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!instance_ptr) {
        throw_illegal_state(env, "glfwCreateWindowSurface: instance not initialized");
        return 0;
    }
    if (!window_ptr) {
        throw_illegal_state(env, "glfwCreateWindowSurface: window not initialized");
        return 0;
    }

    VkSurfaceKHR surface = VK_NULL_HANDLE;
    VkResult result = glfwCreateWindowSurface(
        reinterpret_cast<VkInstance>(instance_ptr),
        reinterpret_cast<GLFWwindow*>(window_ptr),
        nullptr,
        &surface);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing glfwCreateWindowSurface");
        return 0;
    }
    return reinterpret_cast<jlong>(surface);
}


extern "C" JNIEXPORT jobjectArray JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetRequiredInstanceExtensions(
        JNIEnv* env,
        jclass clazz) {
    uint32_t count = 0;
    const char** extensions = glfwGetRequiredInstanceExtensions(&count);
    if (!extensions) {
        throw_illegal_state(env, "glfwGetRequiredInstanceExtensions: GLFW returned no extensions "
            "(was glfwInit called, and does this platform support Vulkan?)");
        return nullptr;
    }
    std::vector<std::string> result;
    result.reserve(count);
    for (uint32_t i = 0; i < count; ++i) {
        result.emplace_back(extensions[i]);
    }
    return make_boxed_string_array(env, result);
}


extern "C" JNIEXPORT jint JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetKey(
        JNIEnv* env,
        jclass clazz,
        jlong window,
        jint key) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);
    int32_t key_val = static_cast<int32_t>(key);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwGetKey: window not initialized");
        return 0;
    }

    return static_cast<jint>(glfwGetKey(reinterpret_cast<GLFWwindow*>(window_ptr), key_val));
}


extern "C" JNIEXPORT jint JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetMouseButton(
        JNIEnv* env,
        jclass clazz,
        jlong window,
        jint button) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);
    int32_t button_val = static_cast<int32_t>(button);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwGetMouseButton: window not initialized");
        return 0;
    }

    return static_cast<jint>(glfwGetMouseButton(reinterpret_cast<GLFWwindow*>(window_ptr), button_val));
}


extern "C" JNIEXPORT jdoubleArray JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwGetCursorPos(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwGetCursorPos: window not initialized");
        return nullptr;
    }

    double x = 0.0, y = 0.0;
    glfwGetCursorPos(reinterpret_cast<GLFWwindow*>(window_ptr), &x, &y);
    jdouble values[2] = { x, y };
    jdoubleArray result = env->NewDoubleArray(2);
    env->SetDoubleArrayRegion(result, 0, 2, values);
    return result;
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwSetScrollCallback(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwSetScrollCallback: window not initialized");
        return;
    }

    glfwSetScrollCallback(reinterpret_cast<GLFWwindow*>(window_ptr), awake_glfwScrollCallback);
}


extern "C" JNIEXPORT jdouble JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwConsumeScrollDeltaY(
        JNIEnv* env,
        jclass clazz,
        jlong window) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwConsumeScrollDeltaY: window not initialized");
        return 0.0;
    }

    double delta = g_scrollAccumulatorY;
    g_scrollAccumulatorY = 0.0;
    return static_cast<jdouble>(delta);
}


// Hand-written, same reasoning as awake_glfwScrollCallback above: doesn't fit the generator's
// "one GLFW call per function" template -- this lazily creates each standard cursor exactly
// once (glfwCreateStandardCursor) and caches it here, keyed by GLFW's own shape constant, so a
// real per-frame glfwSetCursorShape call never re-creates a cursor object. One process-wide
// cache is fine for the same "one GLFW window per process" reason g_scrollAccumulatorY's own
// comment gives. Never freed (glfwTerminate() tears down the whole GLFW context anyway, taking
// every outstanding GLFWcursor* with it).
static std::unordered_map<int32_t, GLFWcursor*> g_cursorCache;

extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanWindow_glfwSetCursorShape(
        JNIEnv* env,
        jclass clazz,
        jlong window,
        jint shape) {
    // --- Marshalling ---
    void* window_ptr = reinterpret_cast<void*>(window);
    int32_t shape_val = static_cast<int32_t>(shape);

    // --- Error handling ---
    if (!window_ptr) {
        throw_illegal_state(env, "glfwSetCursorShape: window not initialized");
        return;
    }

    auto cached = g_cursorCache.find(shape_val);
    GLFWcursor* cursor;
    if (cached != g_cursorCache.end()) {
        cursor = cached->second;
    } else {
        cursor = glfwCreateStandardCursor(shape_val);
        g_cursorCache[shape_val] = cursor;
    }
    glfwSetCursor(reinterpret_cast<GLFWwindow*>(window_ptr), cursor);
}
