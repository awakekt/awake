// GENERATED by jni-binding-generator.py, then hand-edited to fill in the native call
// bodies below. Regenerating overwrites this file; see docs/decisions/D10-codegen-
// derisk-findings.md for the re-merge procedure used to preserve hand-written bodies.
// Filename is fully-qualified (io_github_..._VulkanBuffers_jni.gen.cpp) because both
// androidMain and desktopMain now declare real `actual external fun` for this class --
// jni-binding-generator qualifies output filenames whenever more than one source file
// shares a class name, to avoid one platform's generation silently overwriting the
// other's. Both platforms' Kotlin have identical signatures, so either one regenerates
// byte-identical output; this file is shared and compiled into both android-native's and
// desktop-native's CMake builds unchanged.
// Source: VulkanBuffers.kt
//
// NOTE on offset/memoryOffset params below: jni-binding-generator treats every
// function-level `Long` parameter as an opaque handle (adds a "not initialized"
// null-check), which is correct for real handles (device/buffer/memory/commandBuffer)
// but wrong for a plain numeric byte offset, where 0 is a valid and common value. The
// bogus null-checks for `offset`/`memoryOffset` params have been removed below.

#include <jni.h>
#include <optional>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>
#include <cstdint>
#include <cstring>
#include <vulkan/vulkan.h>

#include "jni-utils.h"
#include "exception_utils.h"

// --- Struct marshalling: VkSharingMode ---
struct JNI_VkSharingMode {
    int32_t value;
};

inline JNI_VkSharingMode extract_VkSharingMode(JNIEnv* env, jobject obj) {
    JNI_VkSharingMode out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_value = env->GetFieldID(cls, "value", "I");
    env->DeleteLocalRef(cls);
    out.value = static_cast<int32_t>(env->GetIntField(obj, fid_value));
    return out;
}

inline jobject make_VkSharingMode(JNIEnv* env, const JNI_VkSharingMode& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/enums/VkSharingMode");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(I)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.value);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}

// --- Struct marshalling: VkBufferCreateInfo ---
struct JNI_VkBufferCreateInfo {
    int64_t size;
    int32_t usage;
    int32_t flags;
    int32_t sharingMode;
};

inline JNI_VkBufferCreateInfo extract_VkBufferCreateInfo(JNIEnv* env, jobject obj) {
    JNI_VkBufferCreateInfo out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_size = env->GetFieldID(cls, "size", "J");
    jfieldID fid_usage = env->GetFieldID(cls, "usage", "I");
    jfieldID fid_flags = env->GetFieldID(cls, "flags", "I");
    jfieldID fid_sharingMode = env->GetFieldID(cls, "sharingMode", "Lio/github/ronjunevaldoz/awake/vulkan/enums/VkSharingMode;");
    env->DeleteLocalRef(cls);
    out.size = static_cast<int64_t>(env->GetLongField(obj, fid_size));
    out.usage = static_cast<int32_t>(env->GetIntField(obj, fid_usage));
    out.flags = static_cast<int32_t>(env->GetIntField(obj, fid_flags));
    {
        jobject _jenum = env->GetObjectField(obj, fid_sharingMode);
        out.sharingMode = enum_ordinal(env, _jenum);
        if (_jenum) env->DeleteLocalRef(_jenum);
    }
    return out;
}

inline jobject make_VkBufferCreateInfo(JNIEnv* env, const JNI_VkBufferCreateInfo& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/models/info/VkBufferCreateInfo");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(JIILio/github/ronjunevaldoz/awake/vulkan/enums/VkSharingMode;)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject _jenum_sharingMode = enum_from_ordinal(env, "io/github/ronjunevaldoz/awake/vulkan/enums/VkSharingMode", val.sharingMode);
    if (!_jenum_sharingMode) {  return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.size, val.usage, val.flags, _jenum_sharingMode);
    env->DeleteLocalRef(_jenum_sharingMode);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}

// --- Struct marshalling: VkMemoryAllocateInfo ---
struct JNI_VkMemoryAllocateInfo {
    int64_t allocationSize;
    int32_t memoryTypeIndex;
};

inline JNI_VkMemoryAllocateInfo extract_VkMemoryAllocateInfo(JNIEnv* env, jobject obj) {
    JNI_VkMemoryAllocateInfo out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_allocationSize = env->GetFieldID(cls, "allocationSize", "J");
    jfieldID fid_memoryTypeIndex = env->GetFieldID(cls, "memoryTypeIndex", "I");
    env->DeleteLocalRef(cls);
    out.allocationSize = static_cast<int64_t>(env->GetLongField(obj, fid_allocationSize));
    out.memoryTypeIndex = static_cast<int32_t>(env->GetIntField(obj, fid_memoryTypeIndex));
    return out;
}

inline jobject make_VkMemoryAllocateInfo(JNIEnv* env, const JNI_VkMemoryAllocateInfo& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/models/info/VkMemoryAllocateInfo");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(JI)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.allocationSize, val.memoryTypeIndex);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}

// --- Struct marshalling: VkMemoryRequirements ---
struct JNI_VkMemoryRequirements {
    int64_t size;
    int64_t alignment;
    int32_t memoryTypeBits;
};

inline JNI_VkMemoryRequirements extract_VkMemoryRequirements(JNIEnv* env, jobject obj) {
    JNI_VkMemoryRequirements out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_size = env->GetFieldID(cls, "size", "J");
    jfieldID fid_alignment = env->GetFieldID(cls, "alignment", "J");
    jfieldID fid_memoryTypeBits = env->GetFieldID(cls, "memoryTypeBits", "I");
    env->DeleteLocalRef(cls);
    out.size = static_cast<int64_t>(env->GetLongField(obj, fid_size));
    out.alignment = static_cast<int64_t>(env->GetLongField(obj, fid_alignment));
    out.memoryTypeBits = static_cast<int32_t>(env->GetIntField(obj, fid_memoryTypeBits));
    return out;
}

inline jobject make_VkMemoryRequirements(JNIEnv* env, const JNI_VkMemoryRequirements& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/models/VkMemoryRequirements");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(JJI)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.size, val.alignment, val.memoryTypeBits);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}


extern "C" JNIEXPORT jlong JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkCreateBuffer(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jobject createInfo) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    JNI_VkBufferCreateInfo createInfo_val = extract_VkBufferCreateInfo(env, createInfo);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkCreateBuffer: device not initialized");
        return 0;
    }
    if (!createInfo) {
        throw_illegal_argument(env, "vkCreateBuffer: createInfo must not be null");
        return 0;
    }

    VkBufferCreateInfo bufferInfo{};
    bufferInfo.sType = VK_STRUCTURE_TYPE_BUFFER_CREATE_INFO;
    bufferInfo.size = static_cast<VkDeviceSize>(createInfo_val.size);
    bufferInfo.usage = static_cast<VkBufferUsageFlags>(createInfo_val.usage);
    bufferInfo.flags = static_cast<VkBufferCreateFlags>(createInfo_val.flags);
    // ordinal matches VkSharingMode's real Vulkan value (0/1) -- see the Phase 1d
    // enum-marshalling hazard note in docs/MVP_PLAN.md before doing this for any
    // other enum.
    bufferInfo.sharingMode = static_cast<VkSharingMode>(createInfo_val.sharingMode);

    VkBuffer buffer = VK_NULL_HANDLE;
    VkResult result = vkCreateBuffer(
        reinterpret_cast<VkDevice>(device_ptr), &bufferInfo, nullptr, &buffer);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkCreateBuffer");
        return 0;
    }
    return reinterpret_cast<jlong>(buffer);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkDestroyBuffer(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong buffer) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* buffer_ptr = reinterpret_cast<void*>(buffer);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkDestroyBuffer: device not initialized");
        return;
    }
    if (!buffer_ptr) {
        throw_illegal_state(env, "vkDestroyBuffer: buffer not initialized");
        return;
    }

    vkDestroyBuffer(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkBuffer>(buffer_ptr),
        nullptr);
}


extern "C" JNIEXPORT jobject JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkGetBufferMemoryRequirements(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong buffer) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* buffer_ptr = reinterpret_cast<void*>(buffer);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkGetBufferMemoryRequirements: device not initialized");
        return nullptr;
    }
    if (!buffer_ptr) {
        throw_illegal_state(env, "vkGetBufferMemoryRequirements: buffer not initialized");
        return nullptr;
    }

    VkMemoryRequirements requirements{};
    vkGetBufferMemoryRequirements(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkBuffer>(buffer_ptr),
        &requirements);

    JNI_VkMemoryRequirements result{};
    result.size = static_cast<int64_t>(requirements.size);
    result.alignment = static_cast<int64_t>(requirements.alignment);
    result.memoryTypeBits = static_cast<int32_t>(requirements.memoryTypeBits);
    return make_VkMemoryRequirements(env, result);
}


extern "C" JNIEXPORT jint JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_findMemoryType(
        JNIEnv* env,
        jclass clazz,
        jlong physicalDevice,
        jint typeFilter,
        jint properties) {
    // --- Marshalling ---
    void* physicalDevice_ptr = reinterpret_cast<void*>(physicalDevice);
    int32_t typeFilter_val = static_cast<int32_t>(typeFilter);
    int32_t properties_val = static_cast<int32_t>(properties);

    // --- Error handling ---
    if (!physicalDevice_ptr) {
        throw_illegal_state(env, "findMemoryType: physicalDevice not initialized");
        return 0;
    }

    VkPhysicalDeviceMemoryProperties memProperties{};
    vkGetPhysicalDeviceMemoryProperties(
        reinterpret_cast<VkPhysicalDevice>(physicalDevice_ptr), &memProperties);

    for (uint32_t i = 0; i < memProperties.memoryTypeCount; ++i) {
        bool typeAllowed = (static_cast<uint32_t>(typeFilter_val) & (1u << i)) != 0;
        bool hasProperties = (memProperties.memoryTypes[i].propertyFlags
            & static_cast<VkMemoryPropertyFlags>(properties_val)) == static_cast<VkMemoryPropertyFlags>(properties_val);
        if (typeAllowed && hasProperties) {
            return static_cast<jint>(i);
        }
    }
    return -1;
}


extern "C" JNIEXPORT jlong JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkAllocateMemory(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jobject allocateInfo) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    JNI_VkMemoryAllocateInfo allocateInfo_val = extract_VkMemoryAllocateInfo(env, allocateInfo);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkAllocateMemory: device not initialized");
        return 0;
    }
    if (!allocateInfo) {
        throw_illegal_argument(env, "vkAllocateMemory: allocateInfo must not be null");
        return 0;
    }

    VkMemoryAllocateInfo allocInfo{};
    allocInfo.sType = VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_INFO;
    allocInfo.allocationSize = static_cast<VkDeviceSize>(allocateInfo_val.allocationSize);
    allocInfo.memoryTypeIndex = static_cast<uint32_t>(allocateInfo_val.memoryTypeIndex);

    VkDeviceMemory memory = VK_NULL_HANDLE;
    VkResult result = vkAllocateMemory(
        reinterpret_cast<VkDevice>(device_ptr), &allocInfo, nullptr, &memory);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkAllocateMemory");
        return 0;
    }
    return reinterpret_cast<jlong>(memory);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkFreeMemory(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong memory) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* memory_ptr = reinterpret_cast<void*>(memory);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkFreeMemory: device not initialized");
        return;
    }
    if (!memory_ptr) {
        throw_illegal_state(env, "vkFreeMemory: memory not initialized");
        return;
    }

    vkFreeMemory(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkDeviceMemory>(memory_ptr),
        nullptr);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkBindBufferMemory(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong buffer,
        jlong memory,
        jlong memoryOffset) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* buffer_ptr = reinterpret_cast<void*>(buffer);
    void* memory_ptr = reinterpret_cast<void*>(memory);
    // memoryOffset is a plain byte offset, not a handle -- 0 is valid and common.
    // (jni-binding-generator treats every function-level Long as a handle by
    // convention; the bogus null-check it would add here has been omitted.)

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkBindBufferMemory: device not initialized");
        return;
    }
    if (!buffer_ptr) {
        throw_illegal_state(env, "vkBindBufferMemory: buffer not initialized");
        return;
    }
    if (!memory_ptr) {
        throw_illegal_state(env, "vkBindBufferMemory: memory not initialized");
        return;
    }

    VkResult result = vkBindBufferMemory(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkBuffer>(buffer_ptr),
        reinterpret_cast<VkDeviceMemory>(memory_ptr),
        static_cast<VkDeviceSize>(memoryOffset));
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkBindBufferMemory");
    }
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_writeBufferMemoryFloats(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong memory,
        jlong offset,
        jfloatArray data) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* memory_ptr = reinterpret_cast<void*>(memory);
    // offset is a plain byte offset, not a handle -- 0 is valid and common (see the
    // memoryOffset note in vkBindBufferMemory above).
    std::vector<float> data_val = extract_float_array(env, data);
    if (env->ExceptionCheck()) return;

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "writeBufferMemoryFloats: device not initialized");
        return;
    }
    if (!memory_ptr) {
        throw_illegal_state(env, "writeBufferMemoryFloats: memory not initialized");
        return;
    }

    void* mapped = nullptr;
    VkDeviceSize sizeBytes = static_cast<VkDeviceSize>(data_val.size() * sizeof(float));
    VkResult result = vkMapMemory(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkDeviceMemory>(memory_ptr),
        static_cast<VkDeviceSize>(offset),
        sizeBytes,
        0,
        &mapped);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkMapMemory");
        return;
    }
    std::memcpy(mapped, data_val.data(), static_cast<size_t>(sizeBytes));
    vkUnmapMemory(reinterpret_cast<VkDevice>(device_ptr), reinterpret_cast<VkDeviceMemory>(memory_ptr));
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_writeBufferMemoryBytes(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong memory,
        jlong offset,
        jbyteArray data) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* memory_ptr = reinterpret_cast<void*>(memory);
    // offset is a plain byte offset, not a handle -- 0 is valid and common (see the
    // memoryOffset note in vkBindBufferMemory above).
    std::vector<uint8_t> data_val = extract_byte_array(env, data);
    if (env->ExceptionCheck()) return;

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "writeBufferMemoryBytes: device not initialized");
        return;
    }
    if (!memory_ptr) {
        throw_illegal_state(env, "writeBufferMemoryBytes: memory not initialized");
        return;
    }

    void* mapped = nullptr;
    VkDeviceSize sizeBytes = static_cast<VkDeviceSize>(data_val.size());
    VkResult result = vkMapMemory(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkDeviceMemory>(memory_ptr),
        static_cast<VkDeviceSize>(offset),
        sizeBytes,
        0,
        &mapped);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkMapMemory");
        return;
    }
    std::memcpy(mapped, data_val.data(), static_cast<size_t>(sizeBytes));
    vkUnmapMemory(reinterpret_cast<VkDevice>(device_ptr), reinterpret_cast<VkDeviceMemory>(memory_ptr));
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkCmdBindVertexBuffers(
        JNIEnv* env,
        jclass clazz,
        jlong commandBuffer,
        jint firstBinding,
        jlongArray buffers,
        jlongArray offsets) {
    // --- Marshalling ---
    void* commandBuffer_ptr = reinterpret_cast<void*>(commandBuffer);
    int32_t firstBinding_val = static_cast<int32_t>(firstBinding);
    std::vector<int64_t> buffers_val = extract_long_array(env, buffers);
    if (env->ExceptionCheck()) return;
    std::vector<int64_t> offsets_val = extract_long_array(env, offsets);
    if (env->ExceptionCheck()) return;

    // --- Error handling ---
    if (!commandBuffer_ptr) {
        throw_illegal_state(env, "vkCmdBindVertexBuffers: commandBuffer not initialized");
        return;
    }
    if (buffers_val.size() != offsets_val.size()) {
        throw_illegal_argument(env, "vkCmdBindVertexBuffers: buffers and offsets must be the same size");
        return;
    }

    std::vector<VkBuffer> vkBuffers(buffers_val.size());
    std::vector<VkDeviceSize> vkOffsets(offsets_val.size());
    for (size_t i = 0; i < buffers_val.size(); ++i) {
        vkBuffers[i] = reinterpret_cast<VkBuffer>(buffers_val[i]);
        vkOffsets[i] = static_cast<VkDeviceSize>(offsets_val[i]);
    }

    vkCmdBindVertexBuffers(
        reinterpret_cast<VkCommandBuffer>(commandBuffer_ptr),
        static_cast<uint32_t>(firstBinding_val),
        static_cast<uint32_t>(vkBuffers.size()),
        vkBuffers.data(),
        vkOffsets.data());
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkCmdBindIndexBuffer(
        JNIEnv* env,
        jclass clazz,
        jlong commandBuffer,
        jlong buffer,
        jlong offset,
        jint indexType) {
    // --- Marshalling ---
    void* commandBuffer_ptr = reinterpret_cast<void*>(commandBuffer);
    void* buffer_ptr = reinterpret_cast<void*>(buffer);
    // offset is a plain byte offset, not a handle -- 0 is valid and common.
    int32_t indexType_val = static_cast<int32_t>(indexType);

    // --- Error handling ---
    if (!commandBuffer_ptr) {
        throw_illegal_state(env, "vkCmdBindIndexBuffer: commandBuffer not initialized");
        return;
    }
    if (!buffer_ptr) {
        throw_illegal_state(env, "vkCmdBindIndexBuffer: buffer not initialized");
        return;
    }

    vkCmdBindIndexBuffer(
        reinterpret_cast<VkCommandBuffer>(commandBuffer_ptr),
        reinterpret_cast<VkBuffer>(buffer_ptr),
        static_cast<VkDeviceSize>(offset),
        static_cast<VkIndexType>(indexType_val));
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkCmdCopyBuffer(
        JNIEnv* env,
        jclass clazz,
        jlong commandBuffer,
        jlong srcBuffer,
        jlong dstBuffer,
        jlong size) {
    // --- Marshalling ---
    void* commandBuffer_ptr = reinterpret_cast<void*>(commandBuffer);
    void* srcBuffer_ptr = reinterpret_cast<void*>(srcBuffer);
    void* dstBuffer_ptr = reinterpret_cast<void*>(dstBuffer);
    // size is a plain byte count, not a handle -- 0 would be a legal (if useless) copy.

    // --- Error handling ---
    if (!commandBuffer_ptr) {
        throw_illegal_state(env, "vkCmdCopyBuffer: commandBuffer not initialized");
        return;
    }
    if (!srcBuffer_ptr) {
        throw_illegal_state(env, "vkCmdCopyBuffer: srcBuffer not initialized");
        return;
    }
    if (!dstBuffer_ptr) {
        throw_illegal_state(env, "vkCmdCopyBuffer: dstBuffer not initialized");
        return;
    }

    // Single-region copy -- srcOffset/dstOffset are always 0 for the staging-buffer upload
    // pattern this exists for (see VulkanBuffers.kt's doc comment on the Kotlin side).
    VkBufferCopy copyRegion{};
    copyRegion.srcOffset = 0;
    copyRegion.dstOffset = 0;
    copyRegion.size = static_cast<VkDeviceSize>(size);

    vkCmdCopyBuffer(
        reinterpret_cast<VkCommandBuffer>(commandBuffer_ptr),
        reinterpret_cast<VkBuffer>(srcBuffer_ptr),
        reinterpret_cast<VkBuffer>(dstBuffer_ptr),
        1,
        &copyRegion);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkCmdDrawIndexed(
        JNIEnv* env,
        jclass clazz,
        jlong commandBuffer,
        jint indexCount,
        jint instanceCount,
        jint firstIndex,
        jint vertexOffset,
        jint firstInstance) {
    // --- Marshalling ---
    void* commandBuffer_ptr = reinterpret_cast<void*>(commandBuffer);
    int32_t indexCount_val = static_cast<int32_t>(indexCount);
    int32_t instanceCount_val = static_cast<int32_t>(instanceCount);
    int32_t firstIndex_val = static_cast<int32_t>(firstIndex);
    int32_t vertexOffset_val = static_cast<int32_t>(vertexOffset);
    int32_t firstInstance_val = static_cast<int32_t>(firstInstance);

    // --- Error handling ---
    if (!commandBuffer_ptr) {
        throw_illegal_state(env, "vkCmdDrawIndexed: commandBuffer not initialized");
        return;
    }

    vkCmdDrawIndexed(
        reinterpret_cast<VkCommandBuffer>(commandBuffer_ptr),
        static_cast<uint32_t>(indexCount_val),
        static_cast<uint32_t>(instanceCount_val),
        static_cast<uint32_t>(firstIndex_val),
        vertexOffset_val,
        static_cast<uint32_t>(firstInstance_val));
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_vkDeviceWaitIdle(
        JNIEnv* env,
        jclass clazz,
        jlong device) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkDeviceWaitIdle: device not initialized");
        return;
    }

    vkDeviceWaitIdle(reinterpret_cast<VkDevice>(device_ptr));
}


// Hand-written (not jni-binding-generator output): inverse of writeBufferMemoryBytes above
// (map->memcpy-out->unmap instead of map->memcpy-in->unmap), for offscreen render-target
// CPU readback (Renderer.readPixels). Returns a fresh jbyteArray of `size` bytes.
extern "C" JNIEXPORT jbyteArray JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanBuffers_readBufferMemoryBytes(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong memory,
        jlong offset,
        jint size) {
    void* device_ptr = reinterpret_cast<void*>(device);
    void* memory_ptr = reinterpret_cast<void*>(memory);

    if (!device_ptr) {
        throw_illegal_state(env, "readBufferMemoryBytes: device not initialized");
        return nullptr;
    }
    if (!memory_ptr) {
        throw_illegal_state(env, "readBufferMemoryBytes: memory not initialized");
        return nullptr;
    }

    void* mapped = nullptr;
    VkDeviceSize sizeBytes = static_cast<VkDeviceSize>(size);
    VkResult result = vkMapMemory(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkDeviceMemory>(memory_ptr),
        static_cast<VkDeviceSize>(offset),
        sizeBytes,
        0,
        &mapped);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkMapMemory");
        return nullptr;
    }
    jbyteArray out = env->NewByteArray(size);
    env->SetByteArrayRegion(out, 0, size, reinterpret_cast<const jbyte*>(mapped));
    vkUnmapMemory(reinterpret_cast<VkDevice>(device_ptr), reinterpret_cast<VkDeviceMemory>(memory_ptr));
    return out;
}
