// GENERATED by jni-binding-generator.py, then hand-edited to fill in the native call
// bodies below. Regenerating overwrites this file; see the header comment in
// io_github_..._VulkanBuffers_jni.gen.cpp for why the filename is fully qualified, and
// docs/decisions/D10-codegen-derisk-findings.md for the re-merge convention.
// Source: VulkanImages.kt
//
// NOTE on memoryOffset below: same plain-byte-offset exception as vkBindBufferMemory in
// VulkanBuffers -- 0 is valid and common, so the bogus "not initialized" null-check
// jni-binding-generator adds for every function-level Long has been removed.

#include <jni.h>
#include <optional>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <vector>
#include <cstdint>
#include <vulkan/vulkan.h>

#include "jni-utils.h"
#include "exception_utils.h"

// --- Struct marshalling: VkBufferImageCopy ---
struct JNI_VkBufferImageCopy {
    int32_t imageWidth;
    int32_t imageHeight;
    int64_t bufferOffset;
    int32_t bufferRowLength;
    int32_t bufferImageHeight;
    int32_t mipLevel;
    int32_t baseArrayLayer;
    int32_t layerCount;
};

inline JNI_VkBufferImageCopy extract_VkBufferImageCopy(JNIEnv* env, jobject obj) {
    JNI_VkBufferImageCopy out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_imageWidth = env->GetFieldID(cls, "imageWidth", "I");
    jfieldID fid_imageHeight = env->GetFieldID(cls, "imageHeight", "I");
    jfieldID fid_bufferOffset = env->GetFieldID(cls, "bufferOffset", "J");
    jfieldID fid_bufferRowLength = env->GetFieldID(cls, "bufferRowLength", "I");
    jfieldID fid_bufferImageHeight = env->GetFieldID(cls, "bufferImageHeight", "I");
    jfieldID fid_mipLevel = env->GetFieldID(cls, "mipLevel", "I");
    jfieldID fid_baseArrayLayer = env->GetFieldID(cls, "baseArrayLayer", "I");
    jfieldID fid_layerCount = env->GetFieldID(cls, "layerCount", "I");
    env->DeleteLocalRef(cls);
    out.imageWidth = static_cast<int32_t>(env->GetIntField(obj, fid_imageWidth));
    out.imageHeight = static_cast<int32_t>(env->GetIntField(obj, fid_imageHeight));
    out.bufferOffset = static_cast<int64_t>(env->GetLongField(obj, fid_bufferOffset));
    out.bufferRowLength = static_cast<int32_t>(env->GetIntField(obj, fid_bufferRowLength));
    out.bufferImageHeight = static_cast<int32_t>(env->GetIntField(obj, fid_bufferImageHeight));
    out.mipLevel = static_cast<int32_t>(env->GetIntField(obj, fid_mipLevel));
    out.baseArrayLayer = static_cast<int32_t>(env->GetIntField(obj, fid_baseArrayLayer));
    out.layerCount = static_cast<int32_t>(env->GetIntField(obj, fid_layerCount));
    return out;
}

inline jobject make_VkBufferImageCopy(JNIEnv* env, const JNI_VkBufferImageCopy& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/models/info/VkBufferImageCopy");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(IIJIIIII)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.imageWidth, val.imageHeight, val.bufferOffset, val.bufferRowLength, val.bufferImageHeight, val.mipLevel, val.baseArrayLayer, val.layerCount);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}

// --- Struct marshalling: VkImageCreateInfo ---
struct JNI_VkImageCreateInfo {
    int32_t width;
    int32_t height;
    int32_t format;
    int32_t usage;
    int32_t imageType;
    int32_t tiling;
    int32_t initialLayout;
    int32_t sharingMode;
    int32_t mipLevels;
    int32_t arrayLayers;
    int32_t samples;
    int32_t flags;
};

inline JNI_VkImageCreateInfo extract_VkImageCreateInfo(JNIEnv* env, jobject obj) {
    JNI_VkImageCreateInfo out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_width = env->GetFieldID(cls, "width", "I");
    jfieldID fid_height = env->GetFieldID(cls, "height", "I");
    jfieldID fid_format = env->GetFieldID(cls, "format", "I");
    jfieldID fid_usage = env->GetFieldID(cls, "usage", "I");
    jfieldID fid_imageType = env->GetFieldID(cls, "imageType", "I");
    jfieldID fid_tiling = env->GetFieldID(cls, "tiling", "I");
    jfieldID fid_initialLayout = env->GetFieldID(cls, "initialLayout", "I");
    jfieldID fid_sharingMode = env->GetFieldID(cls, "sharingMode", "I");
    jfieldID fid_mipLevels = env->GetFieldID(cls, "mipLevels", "I");
    jfieldID fid_arrayLayers = env->GetFieldID(cls, "arrayLayers", "I");
    jfieldID fid_samples = env->GetFieldID(cls, "samples", "I");
    jfieldID fid_flags = env->GetFieldID(cls, "flags", "I");
    env->DeleteLocalRef(cls);
    out.width = static_cast<int32_t>(env->GetIntField(obj, fid_width));
    out.height = static_cast<int32_t>(env->GetIntField(obj, fid_height));
    out.format = static_cast<int32_t>(env->GetIntField(obj, fid_format));
    out.usage = static_cast<int32_t>(env->GetIntField(obj, fid_usage));
    out.imageType = static_cast<int32_t>(env->GetIntField(obj, fid_imageType));
    out.tiling = static_cast<int32_t>(env->GetIntField(obj, fid_tiling));
    out.initialLayout = static_cast<int32_t>(env->GetIntField(obj, fid_initialLayout));
    out.sharingMode = static_cast<int32_t>(env->GetIntField(obj, fid_sharingMode));
    out.mipLevels = static_cast<int32_t>(env->GetIntField(obj, fid_mipLevels));
    out.arrayLayers = static_cast<int32_t>(env->GetIntField(obj, fid_arrayLayers));
    out.samples = static_cast<int32_t>(env->GetIntField(obj, fid_samples));
    out.flags = static_cast<int32_t>(env->GetIntField(obj, fid_flags));
    return out;
}

inline jobject make_VkImageCreateInfo(JNIEnv* env, const JNI_VkImageCreateInfo& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/models/info/VkImageCreateInfo");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(IIIIIIIIIIII)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.width, val.height, val.format, val.usage, val.imageType, val.tiling, val.initialLayout, val.sharingMode, val.mipLevels, val.arrayLayers, val.samples, val.flags);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}

// --- Struct marshalling: VkMemoryRequirements ---
struct JNI_VkMemoryRequirements {
    int64_t size;
    int64_t alignment;
    int32_t memoryTypeBits;
};

inline JNI_VkMemoryRequirements extract_VkMemoryRequirements(JNIEnv* env, jobject obj) {
    JNI_VkMemoryRequirements out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_size = env->GetFieldID(cls, "size", "J");
    jfieldID fid_alignment = env->GetFieldID(cls, "alignment", "J");
    jfieldID fid_memoryTypeBits = env->GetFieldID(cls, "memoryTypeBits", "I");
    env->DeleteLocalRef(cls);
    out.size = static_cast<int64_t>(env->GetLongField(obj, fid_size));
    out.alignment = static_cast<int64_t>(env->GetLongField(obj, fid_alignment));
    out.memoryTypeBits = static_cast<int32_t>(env->GetIntField(obj, fid_memoryTypeBits));
    return out;
}

inline jobject make_VkMemoryRequirements(JNIEnv* env, const JNI_VkMemoryRequirements& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/models/VkMemoryRequirements");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(JJI)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.size, val.alignment, val.memoryTypeBits);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}

// --- Struct marshalling: VkSamplerCreateInfo ---
struct JNI_VkSamplerCreateInfo {
    int32_t magFilter;
    int32_t minFilter;
    int32_t addressModeU;
    int32_t addressModeV;
    int32_t addressModeW;
    bool anisotropyEnable;
    float maxAnisotropy;
    int32_t borderColor;
    bool unnormalizedCoordinates;
    int32_t mipmapMode;
    float minLod;
    float maxLod;
};

inline JNI_VkSamplerCreateInfo extract_VkSamplerCreateInfo(JNIEnv* env, jobject obj) {
    JNI_VkSamplerCreateInfo out{};
    if (!obj) return out;
    jclass cls = env->GetObjectClass(obj);
    jfieldID fid_magFilter = env->GetFieldID(cls, "magFilter", "I");
    jfieldID fid_minFilter = env->GetFieldID(cls, "minFilter", "I");
    jfieldID fid_addressModeU = env->GetFieldID(cls, "addressModeU", "I");
    jfieldID fid_addressModeV = env->GetFieldID(cls, "addressModeV", "I");
    jfieldID fid_addressModeW = env->GetFieldID(cls, "addressModeW", "I");
    jfieldID fid_anisotropyEnable = env->GetFieldID(cls, "anisotropyEnable", "Z");
    jfieldID fid_maxAnisotropy = env->GetFieldID(cls, "maxAnisotropy", "F");
    jfieldID fid_borderColor = env->GetFieldID(cls, "borderColor", "I");
    jfieldID fid_unnormalizedCoordinates = env->GetFieldID(cls, "unnormalizedCoordinates", "Z");
    jfieldID fid_mipmapMode = env->GetFieldID(cls, "mipmapMode", "I");
    jfieldID fid_minLod = env->GetFieldID(cls, "minLod", "F");
    jfieldID fid_maxLod = env->GetFieldID(cls, "maxLod", "F");
    env->DeleteLocalRef(cls);
    out.magFilter = static_cast<int32_t>(env->GetIntField(obj, fid_magFilter));
    out.minFilter = static_cast<int32_t>(env->GetIntField(obj, fid_minFilter));
    out.addressModeU = static_cast<int32_t>(env->GetIntField(obj, fid_addressModeU));
    out.addressModeV = static_cast<int32_t>(env->GetIntField(obj, fid_addressModeV));
    out.addressModeW = static_cast<int32_t>(env->GetIntField(obj, fid_addressModeW));
    out.anisotropyEnable = (env->GetBooleanField(obj, fid_anisotropyEnable) == JNI_TRUE);
    out.maxAnisotropy = static_cast<float>(env->GetFloatField(obj, fid_maxAnisotropy));
    out.borderColor = static_cast<int32_t>(env->GetIntField(obj, fid_borderColor));
    out.unnormalizedCoordinates = (env->GetBooleanField(obj, fid_unnormalizedCoordinates) == JNI_TRUE);
    out.mipmapMode = static_cast<int32_t>(env->GetIntField(obj, fid_mipmapMode));
    out.minLod = static_cast<float>(env->GetFloatField(obj, fid_minLod));
    out.maxLod = static_cast<float>(env->GetFloatField(obj, fid_maxLod));
    return out;
}

inline jobject make_VkSamplerCreateInfo(JNIEnv* env, const JNI_VkSamplerCreateInfo& val) {
    jclass cls = env->FindClass("io/github/ronjunevaldoz/awake/vulkan/models/info/VkSamplerCreateInfo");
    if (!cls) return nullptr;
    jmethodID ctor = env->GetMethodID(cls, "<init>", "(IIIIIZFIZIFF)V");
    if (!ctor) { env->DeleteLocalRef(cls); return nullptr; }
    jobject result = env->NewObject(cls, ctor, val.magFilter, val.minFilter, val.addressModeU, val.addressModeV, val.addressModeW, static_cast<jboolean>(val.anisotropyEnable ? JNI_TRUE : JNI_FALSE), val.maxAnisotropy, val.borderColor, static_cast<jboolean>(val.unnormalizedCoordinates ? JNI_TRUE : JNI_FALSE), val.mipmapMode, val.minLod, val.maxLod);
    if (!result) { env->DeleteLocalRef(cls); return nullptr; }
    env->DeleteLocalRef(cls);
    return result;
}


extern "C" JNIEXPORT jlong JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkCreateImage(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jobject createInfo) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    JNI_VkImageCreateInfo createInfo_val = extract_VkImageCreateInfo(env, createInfo);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkCreateImage: device not initialized");
        return 0;
    }
    if (!createInfo) {
        throw_illegal_argument(env, "vkCreateImage: createInfo must not be null");
        return 0;
    }

    VkImageCreateInfo imageInfo{};
    imageInfo.sType = VK_STRUCTURE_TYPE_IMAGE_CREATE_INFO;
    imageInfo.imageType = static_cast<VkImageType>(createInfo_val.imageType);
    imageInfo.extent.width = static_cast<uint32_t>(createInfo_val.width);
    imageInfo.extent.height = static_cast<uint32_t>(createInfo_val.height);
    imageInfo.extent.depth = 1;
    imageInfo.mipLevels = static_cast<uint32_t>(createInfo_val.mipLevels);
    imageInfo.arrayLayers = static_cast<uint32_t>(createInfo_val.arrayLayers);
    imageInfo.format = static_cast<VkFormat>(createInfo_val.format);
    imageInfo.tiling = static_cast<VkImageTiling>(createInfo_val.tiling);
    imageInfo.initialLayout = static_cast<VkImageLayout>(createInfo_val.initialLayout);
    imageInfo.usage = static_cast<VkImageUsageFlags>(createInfo_val.usage);
    imageInfo.samples = static_cast<VkSampleCountFlagBits>(createInfo_val.samples);
    imageInfo.sharingMode = static_cast<VkSharingMode>(createInfo_val.sharingMode);
    imageInfo.flags = static_cast<VkImageCreateFlags>(createInfo_val.flags);

    VkImage image = VK_NULL_HANDLE;
    VkResult result = vkCreateImage(
        reinterpret_cast<VkDevice>(device_ptr), &imageInfo, nullptr, &image);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkCreateImage");
        return 0;
    }
    return reinterpret_cast<jlong>(image);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkDestroyImage(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong image) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* image_ptr = reinterpret_cast<void*>(image);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkDestroyImage: device not initialized");
        return;
    }
    if (!image_ptr) {
        throw_illegal_state(env, "vkDestroyImage: image not initialized");
        return;
    }

    vkDestroyImage(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkImage>(image_ptr),
        nullptr);
}


extern "C" JNIEXPORT jobject JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkGetImageMemoryRequirements(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong image) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* image_ptr = reinterpret_cast<void*>(image);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkGetImageMemoryRequirements: device not initialized");
        return nullptr;
    }
    if (!image_ptr) {
        throw_illegal_state(env, "vkGetImageMemoryRequirements: image not initialized");
        return nullptr;
    }

    VkMemoryRequirements requirements{};
    vkGetImageMemoryRequirements(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkImage>(image_ptr),
        &requirements);

    JNI_VkMemoryRequirements result{};
    result.size = static_cast<int64_t>(requirements.size);
    result.alignment = static_cast<int64_t>(requirements.alignment);
    result.memoryTypeBits = static_cast<int32_t>(requirements.memoryTypeBits);
    return make_VkMemoryRequirements(env, result);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkBindImageMemory(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong image,
        jlong memory,
        jlong memoryOffset) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* image_ptr = reinterpret_cast<void*>(image);
    void* memory_ptr = reinterpret_cast<void*>(memory);
    // memoryOffset is a plain byte offset, not a handle -- 0 is valid and common.

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkBindImageMemory: device not initialized");
        return;
    }
    if (!image_ptr) {
        throw_illegal_state(env, "vkBindImageMemory: image not initialized");
        return;
    }
    if (!memory_ptr) {
        throw_illegal_state(env, "vkBindImageMemory: memory not initialized");
        return;
    }

    VkResult result = vkBindImageMemory(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkImage>(image_ptr),
        reinterpret_cast<VkDeviceMemory>(memory_ptr),
        static_cast<VkDeviceSize>(memoryOffset));
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkBindImageMemory");
    }
}


extern "C" JNIEXPORT jlong JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkCreateSampler(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jobject createInfo) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    JNI_VkSamplerCreateInfo createInfo_val = extract_VkSamplerCreateInfo(env, createInfo);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkCreateSampler: device not initialized");
        return 0;
    }
    if (!createInfo) {
        throw_illegal_argument(env, "vkCreateSampler: createInfo must not be null");
        return 0;
    }

    VkSamplerCreateInfo samplerInfo{};
    samplerInfo.sType = VK_STRUCTURE_TYPE_SAMPLER_CREATE_INFO;
    samplerInfo.magFilter = static_cast<VkFilter>(createInfo_val.magFilter);
    samplerInfo.minFilter = static_cast<VkFilter>(createInfo_val.minFilter);
    samplerInfo.addressModeU = static_cast<VkSamplerAddressMode>(createInfo_val.addressModeU);
    samplerInfo.addressModeV = static_cast<VkSamplerAddressMode>(createInfo_val.addressModeV);
    samplerInfo.addressModeW = static_cast<VkSamplerAddressMode>(createInfo_val.addressModeW);
    samplerInfo.anisotropyEnable = createInfo_val.anisotropyEnable ? VK_TRUE : VK_FALSE;
    samplerInfo.maxAnisotropy = createInfo_val.maxAnisotropy;
    samplerInfo.borderColor = static_cast<VkBorderColor>(createInfo_val.borderColor);
    samplerInfo.unnormalizedCoordinates = createInfo_val.unnormalizedCoordinates ? VK_TRUE : VK_FALSE;
    samplerInfo.compareEnable = VK_FALSE;
    samplerInfo.compareOp = VK_COMPARE_OP_ALWAYS;
    samplerInfo.mipmapMode = static_cast<VkSamplerMipmapMode>(createInfo_val.mipmapMode);
    samplerInfo.mipLodBias = 0.0f;
    samplerInfo.minLod = createInfo_val.minLod;
    samplerInfo.maxLod = createInfo_val.maxLod;

    VkSampler sampler = VK_NULL_HANDLE;
    VkResult result = vkCreateSampler(
        reinterpret_cast<VkDevice>(device_ptr), &samplerInfo, nullptr, &sampler);
    if (result != VK_SUCCESS) {
        exception_utils::resultException(env, result,
            "There was a problem executing vkCreateSampler");
        return 0;
    }
    return reinterpret_cast<jlong>(sampler);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkDestroySampler(
        JNIEnv* env,
        jclass clazz,
        jlong device,
        jlong sampler) {
    // --- Marshalling ---
    void* device_ptr = reinterpret_cast<void*>(device);
    void* sampler_ptr = reinterpret_cast<void*>(sampler);

    // --- Error handling ---
    if (!device_ptr) {
        throw_illegal_state(env, "vkDestroySampler: device not initialized");
        return;
    }
    if (!sampler_ptr) {
        throw_illegal_state(env, "vkDestroySampler: sampler not initialized");
        return;
    }

    vkDestroySampler(
        reinterpret_cast<VkDevice>(device_ptr),
        reinterpret_cast<VkSampler>(sampler_ptr),
        nullptr);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkTransitionImageLayout(
        JNIEnv* env,
        jclass clazz,
        jlong commandBuffer,
        jlong image,
        jint oldLayout,
        jint newLayout,
        jint levelCount) {
    // --- Marshalling ---
    void* commandBuffer_ptr = reinterpret_cast<void*>(commandBuffer);
    void* image_ptr = reinterpret_cast<void*>(image);
    int32_t oldLayout_val = static_cast<int32_t>(oldLayout);
    int32_t newLayout_val = static_cast<int32_t>(newLayout);
    int32_t levelCount_val = static_cast<int32_t>(levelCount);

    // --- Error handling ---
    if (!commandBuffer_ptr) {
        throw_illegal_state(env, "vkTransitionImageLayout: commandBuffer not initialized");
        return;
    }
    if (!image_ptr) {
        throw_illegal_state(env, "vkTransitionImageLayout: image not initialized");
        return;
    }

    VkImageLayout oldVkLayout = static_cast<VkImageLayout>(oldLayout_val);
    VkImageLayout newVkLayout = static_cast<VkImageLayout>(newLayout_val);

    VkImageMemoryBarrier barrier{};
    barrier.sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_BARRIER;
    barrier.oldLayout = oldVkLayout;
    barrier.newLayout = newVkLayout;
    barrier.srcQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED;
    barrier.dstQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED;
    barrier.image = reinterpret_cast<VkImage>(image_ptr);
    barrier.subresourceRange.aspectMask = VK_IMAGE_ASPECT_COLOR_BIT;
    barrier.subresourceRange.baseMipLevel = 0;
    // Explicit level count from the caller, not VK_REMAINING_MIP_LEVELS -- that sentinel was
    // tried first and silently transitioned only level 0 on MoltenVK (confirmed via real
    // validation errors: every level above 0 stayed UNDEFINED). A texture with a real mip
    // chain (see Texture.kt) passes its real level count; every other caller (swapchain
    // images, offscreen render targets) passes the default 1.
    barrier.subresourceRange.levelCount = static_cast<uint32_t>(levelCount_val);
    barrier.subresourceRange.baseArrayLayer = 0;
    barrier.subresourceRange.layerCount = 1;

    VkPipelineStageFlags srcStage;
    VkPipelineStageFlags dstStage;

    if (oldVkLayout == VK_IMAGE_LAYOUT_UNDEFINED && newVkLayout == VK_IMAGE_LAYOUT_TRANSFER_DST_OPTIMAL) {
        barrier.srcAccessMask = 0;
        barrier.dstAccessMask = VK_ACCESS_TRANSFER_WRITE_BIT;
        srcStage = VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT;
        dstStage = VK_PIPELINE_STAGE_TRANSFER_BIT;
    } else if (oldVkLayout == VK_IMAGE_LAYOUT_TRANSFER_DST_OPTIMAL && newVkLayout == VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL) {
        barrier.srcAccessMask = VK_ACCESS_TRANSFER_WRITE_BIT;
        barrier.dstAccessMask = VK_ACCESS_SHADER_READ_BIT;
        srcStage = VK_PIPELINE_STAGE_TRANSFER_BIT;
        dstStage = VK_PIPELINE_STAGE_FRAGMENT_SHADER_BIT;
    } else if (oldVkLayout == VK_IMAGE_LAYOUT_COLOR_ATTACHMENT_OPTIMAL && newVkLayout == VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL) {
        // Offscreen render-target post-render transition (Renderer.renderToTexture): the
        // color attachment just finished being written by the render pass, now made
        // sampleable for compositing (Renderer.createMaterial(renderTarget=...)).
        barrier.srcAccessMask = VK_ACCESS_COLOR_ATTACHMENT_WRITE_BIT;
        barrier.dstAccessMask = VK_ACCESS_SHADER_READ_BIT;
        srcStage = VK_PIPELINE_STAGE_COLOR_ATTACHMENT_OUTPUT_BIT;
        dstStage = VK_PIPELINE_STAGE_FRAGMENT_SHADER_BIT;
    } else if (oldVkLayout == VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL && newVkLayout == VK_IMAGE_LAYOUT_TRANSFER_SRC_OPTIMAL) {
        // Offscreen render-target readback (Renderer.readPixels): temporarily leave the
        // sampleable resting state to let vkCmdCopyImageToBuffer read the image.
        barrier.srcAccessMask = VK_ACCESS_SHADER_READ_BIT;
        barrier.dstAccessMask = VK_ACCESS_TRANSFER_READ_BIT;
        srcStage = VK_PIPELINE_STAGE_FRAGMENT_SHADER_BIT;
        dstStage = VK_PIPELINE_STAGE_TRANSFER_BIT;
    } else if (oldVkLayout == VK_IMAGE_LAYOUT_TRANSFER_SRC_OPTIMAL && newVkLayout == VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL) {
        // Restores the sampleable resting state after readPixels' copy completes.
        barrier.srcAccessMask = VK_ACCESS_TRANSFER_READ_BIT;
        barrier.dstAccessMask = VK_ACCESS_SHADER_READ_BIT;
        srcStage = VK_PIPELINE_STAGE_TRANSFER_BIT;
        dstStage = VK_PIPELINE_STAGE_FRAGMENT_SHADER_BIT;
    } else {
        throw_illegal_argument(env, "vkTransitionImageLayout: unsupported layout transition");
        return;
    }

    vkCmdPipelineBarrier(
        reinterpret_cast<VkCommandBuffer>(commandBuffer_ptr),
        srcStage, dstStage,
        0,
        0, nullptr,
        0, nullptr,
        1, &barrier);
}


extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkCmdCopyBufferToImage(
        JNIEnv* env,
        jclass clazz,
        jlong commandBuffer,
        jlong srcBuffer,
        jlong dstImage,
        jobject copy) {
    // --- Marshalling ---
    void* commandBuffer_ptr = reinterpret_cast<void*>(commandBuffer);
    void* srcBuffer_ptr = reinterpret_cast<void*>(srcBuffer);
    void* dstImage_ptr = reinterpret_cast<void*>(dstImage);
    JNI_VkBufferImageCopy copy_val = extract_VkBufferImageCopy(env, copy);

    // --- Error handling ---
    if (!commandBuffer_ptr) {
        throw_illegal_state(env, "vkCmdCopyBufferToImage: commandBuffer not initialized");
        return;
    }
    if (!srcBuffer_ptr) {
        throw_illegal_state(env, "vkCmdCopyBufferToImage: srcBuffer not initialized");
        return;
    }
    if (!dstImage_ptr) {
        throw_illegal_state(env, "vkCmdCopyBufferToImage: dstImage not initialized");
        return;
    }
    if (!copy) {
        throw_illegal_argument(env, "vkCmdCopyBufferToImage: copy must not be null");
        return;
    }

    VkBufferImageCopy region{};
    region.bufferOffset = static_cast<VkDeviceSize>(copy_val.bufferOffset);
    region.bufferRowLength = static_cast<uint32_t>(copy_val.bufferRowLength);
    region.bufferImageHeight = static_cast<uint32_t>(copy_val.bufferImageHeight);
    region.imageSubresource.aspectMask = VK_IMAGE_ASPECT_COLOR_BIT;
    region.imageSubresource.mipLevel = static_cast<uint32_t>(copy_val.mipLevel);
    region.imageSubresource.baseArrayLayer = static_cast<uint32_t>(copy_val.baseArrayLayer);
    region.imageSubresource.layerCount = static_cast<uint32_t>(copy_val.layerCount);
    region.imageOffset = {0, 0, 0};
    region.imageExtent = {
        static_cast<uint32_t>(copy_val.imageWidth),
        static_cast<uint32_t>(copy_val.imageHeight),
        1
    };

    vkCmdCopyBufferToImage(
        reinterpret_cast<VkCommandBuffer>(commandBuffer_ptr),
        reinterpret_cast<VkBuffer>(srcBuffer_ptr),
        reinterpret_cast<VkImage>(dstImage_ptr),
        VK_IMAGE_LAYOUT_TRANSFER_DST_OPTIMAL,
        1, &region);
}


// Hand-written (not jni-binding-generator output): inverse of vkCmdCopyBufferToImage above,
// for offscreen render-target CPU readback (Renderer.readPixels). srcImage is expected in
// TRANSFER_SRC_OPTIMAL layout (the caller transitions it there before this call).
extern "C" JNIEXPORT void JNICALL
Java_io_github_ronjunevaldoz_awake_vulkan_gen_VulkanImages_vkCmdCopyImageToBuffer(
        JNIEnv* env,
        jclass clazz,
        jlong commandBuffer,
        jlong srcImage,
        jlong dstBuffer,
        jobject copy) {
    void* commandBuffer_ptr = reinterpret_cast<void*>(commandBuffer);
    void* srcImage_ptr = reinterpret_cast<void*>(srcImage);
    void* dstBuffer_ptr = reinterpret_cast<void*>(dstBuffer);
    JNI_VkBufferImageCopy copy_val = extract_VkBufferImageCopy(env, copy);

    if (!commandBuffer_ptr) {
        throw_illegal_state(env, "vkCmdCopyImageToBuffer: commandBuffer not initialized");
        return;
    }
    if (!srcImage_ptr) {
        throw_illegal_state(env, "vkCmdCopyImageToBuffer: srcImage not initialized");
        return;
    }
    if (!dstBuffer_ptr) {
        throw_illegal_state(env, "vkCmdCopyImageToBuffer: dstBuffer not initialized");
        return;
    }
    if (!copy) {
        throw_illegal_argument(env, "vkCmdCopyImageToBuffer: copy must not be null");
        return;
    }

    VkBufferImageCopy region2{};
    region2.bufferOffset = static_cast<VkDeviceSize>(copy_val.bufferOffset);
    region2.bufferRowLength = static_cast<uint32_t>(copy_val.bufferRowLength);
    region2.bufferImageHeight = static_cast<uint32_t>(copy_val.bufferImageHeight);
    region2.imageSubresource.aspectMask = VK_IMAGE_ASPECT_COLOR_BIT;
    region2.imageSubresource.mipLevel = static_cast<uint32_t>(copy_val.mipLevel);
    region2.imageSubresource.baseArrayLayer = static_cast<uint32_t>(copy_val.baseArrayLayer);
    region2.imageSubresource.layerCount = static_cast<uint32_t>(copy_val.layerCount);
    region2.imageOffset = {0, 0, 0};
    region2.imageExtent = {
        static_cast<uint32_t>(copy_val.imageWidth),
        static_cast<uint32_t>(copy_val.imageHeight),
        1
    };

    vkCmdCopyImageToBuffer(
        reinterpret_cast<VkCommandBuffer>(commandBuffer_ptr),
        reinterpret_cast<VkImage>(srcImage_ptr),
        VK_IMAGE_LAYOUT_TRANSFER_SRC_OPTIMAL,
        reinterpret_cast<VkBuffer>(dstBuffer_ptr),
        1, &region2);
}
